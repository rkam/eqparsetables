- format output (vs. shell view via option) for boards

- split config into  members, apps, other
  script to create the above
  mod parser to read each one

- pet_owner.txt

- when move data into db, accum unattached pets into pet_owner
  (saves having to re-export via GP)

